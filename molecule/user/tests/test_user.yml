# Molecule managed

---
command:
  su -l -s /bin/bash -c "rbenv --version" {{ .Vars.rbenv.user }}:
    exit-status: 0
    stdout:
      - rbenv {{ .Vars.rbenv.version }}

  rbenv --version:
    exit-status: 127
